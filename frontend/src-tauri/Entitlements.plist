<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.app-sandbox</key>
    <false/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Microphone. Without this entitlement a hardened-runtime build is
         denied audio input by macOS before any application code runs, so
         push-to-talk dictation fails on every machine except one running an
         unsigned development build. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- La caméra, pour les gestes. Même logique que l'entrée audio :
         sans ce droit, une application sandboxée se voit refuser la caméra
         avant d'exécuter la moindre ligne. -->
    <key>com.apple.security.device.camera</key>
    <true/>
    <!-- Apple Events. Every osascript path — reading the frontmost
         application, pasting into it, driving Safari or Mail — is an Apple
         Event. Without this, those tools fail with errAEEventNotPermitted
         instead of prompting for Automation access. -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
</dict>
</plist>
